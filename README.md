# Tienda Online - Proyecto Final Trimestre

Este proyecto es una aplicación web de comercio electrónico (SPA - Single Page Application) desarrollada con Vanilla JavaScript, HTML5 y CSS3 (Metodología BEM). Simula una tienda de ropa conectándose a APIs externas y gestionando usuarios localmente.

## 🚀 Despliegue y Tecnologías

El proyecto sigue la metodología **Git Flow** con ramas `main` y `develop`.

* **Frontend:** HTML5, CSS3 (Grid/Flexbox), JavaScript (ES6 Modules).
* **Backend Simulado:** JSON Server (Usuarios) y FakeStoreAPI (Productos).
* **Despliegue:** Docker (Nginx).

## 📋 Requisitos Previos

Para ejecutar este proyecto localmente necesitas:

1.  [Node.js](https://nodejs.org/) instalado.
2.  Instalar JSON Server globalmente:
    ```bash
    npm install -g json-server
    ```

## 🛠️ Instalación y Ejecución

Sigue estos pasos para levantar el entorno de desarrollo:

### 1. Clonar el repositorio
```bash
git clone <URL_DE_TU_REPO>
cd tienda-ropa-proyecto